import type React from "react";

const InitiativeIndicator: React.FC = () => (
  <span
    className="initiative-indicator initiative"
    role="img"
    aria-label="Has initiative"
  />
);

export default InitiativeIndicator;
